# mini_db
 so I used new oops into from simple map so code can look modular

 adding commandline interface into the code

 adding multiple functionalities some function may have only two inputs while some have one it is best to decide based on the the type of input command

 Permanent disk memory

 Used a fstream to write data into a data.txt file so data can be permanent

 data is loaded into main meory or map when ever we run the program and the load() function is used as an constructor

 have some bugs to fix like white spaces while taking inputs in cli and also in the data.txt file as it is a stream file
